import { _local } from './_local';

export const checkList = {
  checkList:`${_local}/a/exam/list`,//验收单 主列表     -- 正确
  checkListDurgList:`${_local}/a/exam/detail`,//验收单详情列表 formData     -- 与原App一致
  detailbydrugname:`${_local}/a/checkaccept/detailbydrugname`,// 通过药品名称查询 验收单详情 id
  submitCheck:`${_local}/a/examdetail/checkList`,//提交验收
}